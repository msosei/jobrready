# Copy to config/.env and fill values

# Supabase configuration for database and authentication
# SUPABASE_URL: The URL of your Supabase project
SUPABASE_URL=
# SUPABASE_ANON_KEY: The anonymous key for public access to Supabase
SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE: The service role key for admin access to Supabase
SUPABASE_SERVICE_ROLE=

# Stripe configuration for payment processing
# STRIPE_SECRET_KEY: The secret key for Stripe API access
STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET: The webhook secret for verifying Stripe webhook events
STRIPE_WEBHOOK_SECRET=

# OpenAI configuration for AI-powered features
# OPENAI_API_KEY: The API key for accessing OpenAI services
OPENAI_API_KEY=

# Redis configuration for caching and real-time features
# REDIS_URL: The URL for connecting to the Redis instance
REDIS_URL=redis://localhost:6379

# Application URLs for frontend and backend services
# APP_URL: The base URL for the frontend application
APP_URL=http://localhost:3000
# API_URL: The base URL for the backend API
API_URL=http://localhost:8000

# JWT configuration for authentication
# JWT_SECRET: The secret key for signing JWT tokens (should be a random string)
JWT_SECRET=replace-with-random-string

# Optional database URL for local/testing (if not using Supabase client only)
# DATABASE_URL: The URL for connecting to the database directly
DATABASE_URL=

# Frontend public keys for client-side access
# NEXT_PUBLIC_SUPABASE_URL: The public URL of your Supabase project
NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY: The public anonymous key for Supabase access
NEXT_PUBLIC_SUPABASE_ANON_KEY=